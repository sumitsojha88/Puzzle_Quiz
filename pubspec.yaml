name: puzzle_riddle_apti_app
description: A new Flutter application.

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_staggered_grid_view: ^0.3.0
  cupertino_icons: ^1.0.0
  flutter_bloc: ^2.1.1
  websafe_svg: ^1.1.4
  get: ^3.24.0
  firebase_core: "0.7.0"
  firebase_auth: "^0.20.0+1"
  line_awesome_flutter: ^1.0.1
  flutter_screenutil: ^1.0.2
  animated_theme_switcher: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/icons/
    - assets/images/

